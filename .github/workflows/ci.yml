# CI Pipeline for Enterprise Vision AI
name: CI

on:
  push:
    branches: [main, develop, 'release/**']
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

env:
  PYTHON_VERSION: '3.11'
  DOCKER_IMAGE: ghcr.io/${{ github.repository }}
  DOCKER_REGISTRY: ghcr.io

jobs:
  lint:
    name: Linting
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        run: pip install uv

      - name: Install linting dependencies
        run: uv pip install --system flake8 black isort mypy

      - name: Check code formatting (Black)
        run: black --check --diff .

      - name: Check import sorting (isort)
        run: isort --check --diff .

      - name: Run flake8 linter
        run: flake8 .

      - name: Run mypy type checker (non-blocking)
        run: mypy . --ignore-missing-imports --no-error-summary --exclude huggingface_space --exclude venv || true

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12']
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        run: uv pip install --system -e ".[dev]"

      - name: Run pytest
        run: pytest --cov=src/enterprise_vision_ai --cov=services --cov=pages --cov-report=xml --cov-report=term-missing

      - name: Upload coverage to Codecov
        if: matrix.python-version == '3.11'
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          flags: unittests
          name: codecov-umbrella

  integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        run: uv pip install --system -e ".[dev]"

      - name: Test package imports
        run: |
          python -c "import app; print('App import OK')"
          python -c "import pages.defect; print('Defect page import OK')"
          python -c "import pages.ore; print('Ore page import OK')"
          python -c "from services.utils import draw_annotations; print('Services utils OK')"
          python -c "from enterprise_vision_ai import load_model, load_dataset; print('Package OK')"

      - name: Verify directories exist
        run: |
          ls -la datasets/ || echo "No datasets directory"
          ls -la models/ || echo "No models directory"

  docker:
    name: Docker Build Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: ${{ env.DOCKER_IMAGE }}:test
          cache-from: type=gha
          cache-to: type=gha,mode=max
          load: true

      - name: Verify Docker image
        run: |
          docker run --rm ${{ env.DOCKER_IMAGE }}:test python3 --version
          docker run --rm ${{ env.DOCKER_IMAGE }}:test python3 -c "import streamlit; print('Streamlit OK')"

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Bandit security scan
        run: |
          pip install bandit
          bandit -r . -x tests --exclude tests,.git,venv,build,dist || true

      - name: Check for vulnerabilities with pip-audit
        run: |
          pip install pip-audit
          pip-audit -r requirements.txt || true

  notify:
    name: Notify
    needs: [lint, test, integration, docker, security]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Create status summary
        run: |
          echo "## CI Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Job | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Linting | ${{ needs.lint.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Unit Tests | ${{ needs.test.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Integration Tests | ${{ needs.integration.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Docker Build | ${{ needs.docker.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Security Scan | ${{ needs.security.result }} |" >> $GITHUB_STEP_SUMMARY
